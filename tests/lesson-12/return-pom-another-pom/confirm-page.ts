import { Page } from '@playwright/test';

export class ConfirmPage {
    page: Page;

    constructor(page: Page) {
        this.page = page;
    }

    async getInfoOrder() {
        // code

        const info = {

        }

        return info;
    }
}